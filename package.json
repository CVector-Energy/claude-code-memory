{
  "name": "claude-code-memory",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action: carry Claude Code's auto-memory store between CI runs",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/restore.js src/save.js --bundle --platform=node --target=node24 --format=esm --outdir=dist --out-extension:.js=.mjs --banner:js=\"import{createRequire as __ccmRequire}from'node:module';const require=__ccmRequire(import.meta.url);\"",
    "test": "node --test src/*.test.js"
  },
  "dependencies": {
    "@actions/cache": "6.2.0",
    "@actions/core": "3.0.1"
  },
  "devDependencies": {
    "esbuild": "0.27.2"
  },
  "type": "module"
}
